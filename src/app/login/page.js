import Login from "@/components/Auth/Login";

/**
 * LoginPage component serves as the page wrapper for the Login authentication form.
 * It simply renders the Login component.
 */
const LoginPage = () => {
  return <Login />;
};

export default LoginPage;
